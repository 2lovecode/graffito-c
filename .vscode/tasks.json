{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "common-build",
            "command": "/usr/bin/clang",
            "args": [
                "-g",
                // "${fileDirname}/*.c",
                
                "${file}",
                "-o",
                "${workspaceFolder}/build/${fileBasenameNoExtension}",
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ]
}